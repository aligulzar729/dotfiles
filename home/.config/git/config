# Shared git config. Identity and credentials stay in ~/.gitconfig (machine-local).
# git reads this (XDG) and ~/.gitconfig, so anything personal there still wins.

[core]
	pager = delta

[interactive]
	diffFilter = delta --color-only

[delta]
	navigate = true

[merge]
	conflictStyle = zdiff3
